services:
  - type: web
    name: lyvoxis-wallet-bot
    env: python
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: BOT_TOKEN
        value: 你的真實 Bot Token（或填 ${BOT_TOKEN} 用環境變數匯入）