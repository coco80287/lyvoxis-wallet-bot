services:
  - type: web
    name: lyvoxis-wallet-bot
    env: python
    buildCommand: ""
    startCommand: python main.py
    envVars:
      - key: BOT_TOKEN
        value: ${BOT_TOKEN}
    build:
      environment:
        python: 3.11.9